### 函数

> 函数定义

```python
def 函数名称([可选的参数列表]):
    函数体
    [可选的return语句]
```

> 函数调用
 
```python
函数名称(实参列表)
```

> 函数中同时返回多个值

如果想让函数返回多个值，可以借助元组。

```python
def get_a_b_c():
    a = 1
    b = 2
    c = 3
    return (a, b, c)

# 调用
(a, b, c) = get_a_b_c()
```
